function Tutorial(language,data) {
	if (language == 'English'){
		this.zeroTitle = 'Welcome to MetaEden';
		this.zeroContent = 'Until you earn you place here, you will be known only as '+data.kid+', remember this and go forward. MetaEden awaits.';
	}
};